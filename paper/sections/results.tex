\section{Local response hierarchy}
\label{sec:localresults}

The first central test compares the EOS response transverse to the stellar sequence in the I--Love and C--Love planes. Both are constructed from the same stellar configurations and EOS metric, and no empirical relation is used to define either normal direction.

For each reference configuration we compute the covariance-weighted normal response. In a two-observable plane the normal cotangent direction is unique once its normalization metric is declared. If \(G^{AB}\) is the EOS-induced covariance, \(t^A\) the sequence tangent, and \(g_{AB}\) the observable metric, the scalar response is
\begin{equation}
    \sigma_\perp^2
    =
    n_A G^{AB}n_B,
    \qquad
    n_A t^A=0,
    \qquad
    n_A g^{AB}n_B=1.
    \label{eq:normal-rms}
\end{equation}
The same EOS covariance is used when comparing I--Love and C--Love.

\subsection{Development diagnostic}

Before constructing a broad EOS ensemble, we performed a deliberately restricted diagnostic to determine whether the proposed response geometry contains the expected signal at all. The reference model is a relativistic \(\Gamma=2\) polytrope with \(K=100\) in geometrized units. Five Gaussian latent sound-speed modes span \(h\in[0.02,0.55]\). Their coefficient covariance is Eq.~\eqref{eq:eos-covariance} with \(\sigma_a=0.1\) and \(\ell_h=0.08\). We use the Euclidean metric in \((\ln C,\ln\Ibar,\ln\Lamtwo)\) for this diagnostic only.

Table~\ref{tab:pilot-response} reports the resulting local normal RMS responses. Across the tested sequence, the I--Love normal response is smaller than the C--Love response by a factor between \(7.5\) and \(10.8\) in RMS, or roughly \(57\)--\(116\) in variance. This calculation uses one reference EOS, one EOS covariance, one basis resolution, and one observable metric and therefore is not yet evidence for prior-independent universality. Its purpose is narrower: the differential construction recovers a strong hierarchy without inserting an empirical I--Love fit.

\begin{table}[b]
\caption{Restricted response-geometry diagnostic for the \(\Gamma=2\) reference sequence. The quoted RMS values are local first-order responses in logarithmic observable coordinates under the declared latent-field covariance.}
\label{tab:pilot-response}
\begin{ruledtabular}
\begin{tabular}{ccccc}
\(h_c\) & \(C\) & \(\sigma_{\perp}^{I{\rm Love}}\) &
\(\sigma_{\perp}^{C{\rm Love}}\) &
\(\sigma_{\perp}^{I{\rm Love}}/\sigma_{\perp}^{C{\rm Love}}\)\\
\hline
0.08 & 0.0673 & 0.00198 & 0.02133 & 0.0929\\
0.12 & 0.0933 & 0.00254 & 0.02612 & 0.0974\\
0.16 & 0.1154 & 0.00297 & 0.02857 & 0.1040\\
0.20 & 0.1344 & 0.00335 & 0.03007 & 0.1115\\
0.25 & 0.1545 & 0.00382 & 0.03124 & 0.1223\\
0.30 & 0.1713 & 0.00425 & 0.03202 & 0.1328
\end{tabular}
\end{ruledtabular}
\end{table}

At \(h_c=0.16\), the automatically differentiated observable Jacobian was compared component by component with a symmetric finite difference using a coefficient step \(2\times10^{-4}\). The maximum relative discrepancy was \(8.2\times10^{-8}\), with an RMS relative discrepancy \(2.2\times10^{-8}\). This derivative check is specific to the diagnostic configuration; the production analysis repeats it under basis, integration, and EOS changes.

\subsection{Production nodal response and robustness}

We next repeat the calculation using nodal latent-field values,
Eq.~\eqref{eq:nodal-coordinates}, so that increasing the number of EOS
coordinates refines a fixed functional covariance.  At \(h_c=0.16\) for the
\(\Gamma=2\) reference sequence, the I--Love/C--Love normal RMS ratio is
\[
0.10102,\quad 0.10286,\quad 0.10279,\quad 0.10278
\]
for \(5,9,17,\) and \(33\) nodes, respectively.  The 17-node result differs
from the 33-node value by \(3.6\times10^{-5}\) fractionally.  We use 17 nodes
for the remaining robustness tests.

For the Euclidean metric in logarithmic observables, changing the EOS
covariance among squared-exponential, Mat\'ern-\(3/2\), and exponential
kernels and varying \(\ell_h=0.04,0.08,0.16\) gives
\begin{equation}
    0.1024
    \le
    \frac{\sigma_\perp^{I{\rm Love}}}
         {\sigma_\perp^{C{\rm Love}}}
    \le
    0.1034.
    \label{eq:kernel-robustness}
\end{equation}
Thus the order-of-magnitude suppression is insensitive to both the
correlation length and the differentiability of the allowed EOS
perturbations in this controlled reference problem.

Changing the observable normalization metric changes the numerical response
scale, as it must.  A metric that weights the \(\ln\Ibar\) direction four
times more strongly gives RMS ratios \(0.183\)--\(0.185\), while weighting
the \(\ln\Lamtwo\) direction gives \(0.1048\)--\(0.1058\).  The hierarchy
therefore persists under these deliberately nonuniform normalizations,
although the numerical ratio itself is metric dependent.  This should be
distinguished from a coordinate transformation: transforming from
\((\ln C,\ln\Ibar,\ln\Lamtwo)\) to
\((C,\Ibar,\Lamtwo)\) while transforming \(G^{AB}\), \(t^A\), and \(g_{AB}\)
consistently changes the I--Love and C--Love variances by only
\(8.4\times10^{-14}\) and \(9.4\times10^{-16}\) fractionally, respectively.

The hierarchy is also present away from the \(\Gamma=2\) reference.  For
smooth causal relativistic polytropes with
\(\Gamma=1.70,1.85,2.00\) and \(h_c=0.12,0.20,0.30\), spanning
\(C=0.073\)--\(0.171\), we find
\begin{equation}
    0.096
    \le
    \frac{\sigma_\perp^{I{\rm Love}}}
         {\sigma_\perp^{C{\rm Love}}}
    \le
    0.207.
    \label{eq:reference-robustness}
\end{equation}
Even the weakest case therefore retains a factor \(4.8\) suppression in
RMS response.

\begin{table}[t]
\caption{Summary of the production local-response robustness tests.  The
ranges quote the I--Love/C--Love normal RMS ratio.}
\label{tab:response-robustness}
\begin{ruledtabular}
\begin{tabular}{lc}
Test & RMS-ratio result\\
\hline
Nodal refinement, \(N=17\rightarrow33\)
& fractional change \(3.6\times10^{-5}\)\\
SE/Mat\'ern-3/2/exponential, \(\ell_h=0.04\)--0.16
& \(0.1024\)--\(0.1034\)\\
I-weighted observable metric
& \(0.1829\)--\(0.1847\)\\
Love-weighted observable metric
& \(0.1048\)--\(0.1058\)\\
\(\Gamma=1.70\)--2.00 reference sequences
& \(0.096\)--\(0.207\)\\
1024/512 to 2048/1024 numerical resolution
& fractional change \(3.4\times10^{-3}\)
\end{tabular}
\end{ruledtabular}
\end{table}

For the nodal production representation, a nine-node Jacobian at
\(h_c=0.16\) agrees with a symmetric finite difference of step
\(2\times10^{-4}\) to \(6.5\times10^{-8}\) maximum relative error and
\(1.3\times10^{-8}\) RMS relative error.  Increasing the stellar integration
and thermodynamic-reconstruction resolutions from \(1024/512\) to
\(2048/1024\) steps changes the response ratio by \(3.4\times10^{-3}\).
The coarser \(512/256\) calculation differs from the finest result by about
one percent.

These tests establish H1 and H2 for a controlled family of smooth causal
reference EOSs: the known I--Love relation appears as a strongly suppressed
transverse EOS response without entering the construction.  They do not yet
establish the claim over the full physically allowed EOS space.  The next
test therefore moves the background point itself through a broad
nonparametric latent sound-speed ensemble rather than perturbing only around
a small collection of polytropic references.

\section{Integrability and global reconstruction}
\label{sec:global}

The low-response covector field is transported across the stellar domain and integrated without reference to the standard I--Love fitting formula. In the two-dimensional I--Love plane the local normal field is integrable, but a nontrivial test is obtained by embedding the construction in the full \((\ln C,\ln\Ibar,\ln\Lamtwo)\) space and checking path dependence after quotienting the stellar-sequence direction.

\todo{Stage 4: add integrability diagnostic, path-dependence statistic, and reconstructed I--Love curve. Compare to the empirical relation only after reconstruction.}

\section{Curvature and finite EOS scatter}
\label{sec:curvature}

The second-order tensor entering Eq.~\eqref{eq:second-order} is evaluated using directional Hessian-vector products. Its covariance-weighted contraction gives a local prediction for finite EOS scatter after first-order cancellation.

\todo{Stage 5: compare curvature-predicted scatter with direct nonlinear EOS draws over increasing perturbation amplitude.}

\section{Controlled breakdown}
\label{sec:breakdown}

Flexible EOS models and sharp phase-transition-like structure provide a stress test rather than an extension of the universality claim. We will determine whether loss of I--Love accuracy is accompanied by a growing transverse response, loss of integrability, enhanced curvature, or some combination of these effects. The density localization of the functional kernels will be used to identify which EOS regions produce the breakdown.

\todo{Stage 6: insert broad-EOS and phase-transition results, with the same diagnostics used in the baseline analysis.}
